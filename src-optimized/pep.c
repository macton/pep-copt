// pep.c — single translation unit instantiating the OPTIMIZED PEP compressor.
// Starts as a verbatim copy of src/pep.c; the optimization work edits
// src-optimized/pep.h (the compressor). See prompts/create-optimized.md.
#define PEP_IMPLEMENTATION
#include "pep.h"
